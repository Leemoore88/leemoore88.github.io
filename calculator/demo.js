function addNumToInput(anElement) {
    let num = document.getElementById('screen').value;
    num += anElement.innerText;
    document.getElementById('screen').value = num;
}
const calc = {
    
}
